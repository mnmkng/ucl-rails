<ol class="breadcrumb bg-transparent">
  <li class="breadcrumb-item"><%= link_to 'Dashboard', admin_dashboard_path %></li>
  <li class="breadcrumb-item active">Správa místností</li>
</ol>

<div class="jumbotron">
  <div class="container">
    <h1 class="display-4">Správa místností</h1>
  </div>
</div>
<table class="table table-striped table-hover">
  <thead>
  <tr>
    <th>Název</th>
    <th>Kód</th>
    <th>Budova</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @rooms.each do |room| %>
      <tr>
        <td><%= room.title %></td>
        <td><%= room.code %></td>
        <td><%= room.building.title %></td>
        <td>
          <%= link_to admin_room_path(room), class: 'btn btn-primary' do %>
              <%= fa_icon 'info-circle', text: 'Zobraz detail' %>
          <% end %>
        </td>
        <td><%= link_to 'Uprav', edit_admin_room_path(room), class: 'btn btn-info' %></td>
        <td><%= link_to 'Smaž', admin_room_path(room), class: 'btn btn-danger', method: :delete, data: {confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to new_admin_room_path, class: 'btn btn-primary' do %>
    <%= fa_icon 'plus', text: 'Nová místnost' %>
<% end %>
