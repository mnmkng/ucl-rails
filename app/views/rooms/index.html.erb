<main>

  <ol class="breadcrumb bg-transparent">
    <li class="breadcrumb-item active">Seznam místností</li>
  </ol>

  <div class="jumbotron">
    <div class="container">
      <h1 class="display-4">Seznam místností v budovách</h1>
      <p>Rezervujte si svou místnost ještě dnes!</p>
    </div>
  </div>

  <div class="container">

    <% if @buildings.empty? %>
        <h2>Žádné budovy nebyly nalezeny.</h2>
    <% else %>
        <% @buildings.each do |building| %>

            <h2><%= building.title %></h2>

            <% if building.rooms.empty? %>
                <p>Žádné místnosti nebyly nalezeny.</p>
            <% else %>
                <table class="table table-hover">
                  <thead>
                  <tr>
                    <th>Název</th>
                    <th>Kód</th>
                    <th colspan="1"></th>
                  </tr>
                  </thead>
                  <tbody>
                  <% building.rooms.each do |room| %>
                      <tr>
                        <td><%= room.title %></td>
                        <td><%= room.code %></td>
                        <td class="text-sm-right"><%= link_to room, class: 'btn btn-primary' do %>
                              <%= fa_icon 'info-circle', text: 'Zobraz detail' %>
                          <% end %>
                        </td>
                      </tr>

                  <% end %>
                  </tbody>
                </table>
            <% end %>

        <% end %>

  <% end %>




  </div>

</main>




